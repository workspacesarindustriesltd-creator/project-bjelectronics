fund=false
audit=true
# Hostinger builds with NODE_ENV=production, which normally omits devDependencies.
# Vite, React, and the browser build toolchain are required during the build phase.
include=dev
